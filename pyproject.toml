[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "transport-equations"
version = "0.1.0"
description = "Transport equations solvers and drivers"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
